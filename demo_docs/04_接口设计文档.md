# 文档4：接口设计文档
## 1. SDK对外接口（Kotlin）
### 1.1 Player接口
fun prepare(clip: Clip)
fun play()
fun pause()
fun seek(timeUs: Long)
fun stop()
fun export(config: ExportConfig, callback: ExportCallback)
fun release()

### 1.2 回调接口
interface PlayerCallback {
    onPrepared()
    onProgress(timeUs: Long)
    onCompleted()
    onError(code: Int, msg: String)
}

## 2. JNI接口（Kotlin↔C++）
native fun nativeInit(): Long
native fun nativeRelease(engine: Long)
native fun nativeTick(engine: Long, timeUs: Long)
native fun nativeSetEffect(engine: Long, type: Int, params: FloatArray)
native fun nativeStartExport(engine: Long, config: ExportConfig)
native fun nativeStopExport(engine: Long)

## 3. Native内部接口（C++）
class IRenderGraph {
public:
    virtual void render(Timestamp t) = 0;
    virtual void resize(int w, int h) = 0;
};

class IClock {
public:
    virtual int64_t getCurrentTimeUs() = 0;
    virtual void reset() = 0;
};

## 4. 数据结构接口
struct Layer {
    int layerId;
    TextureRef textureRef;
    Transform transform;
    float opacity;
    BlendMode blendMode;
    int64_t startUs;
    int64_t endUs;
    MaterialRef materialRef;
};